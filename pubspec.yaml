name: offline_assistant
version: 0.1.0+1
publish_to: 'none'

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get_it: ^8.0.3
  flutter_secure_storage: ^9.2.2
  sqflite_sqlcipher: ^3.3.0
  path: ^1.9.0
  path_provider: ^2.1.4
  encrypt: ^5.0.3
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
